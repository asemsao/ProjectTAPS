<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
"http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="project">
	<select id="getAllProject"  resultClass="adins.ace.taps.bean.project.ProjectBean">
		SELECT
			p.project_code AS projectCode,
			p.project_name AS ProjectName,
			p.client AS client,
			p.organization_code AS organizationCode,
			ph.phase_name AS phase,
			p.start_date AS startDate,
			p.end_date AS endDate,
			(TRUNC(SYSDATE) - p.start_date) AS runningDay
		FROM projects p, phases ph
		WHERE p.phase_id = ph.phase_id AND LOWER(flag) = 'active'
	</select>
	
	<select id="getAllProjectMember" resultClass="adins.ace.taps.bean.project.StructureProjectBean" parameterClass="java.lang.String">
		SELECT   
			o.organization_name AS organizationName,
         	p.project_code AS projectCode,
	        p.project_name AS projectName,
	        ps.project_role AS projectRole,
	        e.first_name || ' ' || e.last_name AS assignee,
	        emp.first_name || ' ' || emp.last_name AS directReport
  		FROM   
  			organizations o,
         	projects p,
         	employees e,
         	employees emp,
         	project_structures ps
 		WHERE       
 			p.project_code = ps.project_code
	        AND o.organization_code = p.organization_code
	        AND e.user_domain = ps.assignee_user_domain
	        AND emp.user_domain = ps.directreport_user_domain
			AND ps.project_code = #projectCode#
	</select>
	
<!-- 	<insert id="addProject" parameterClass="adins.ace.taps.bean.project.AddProjectBean" > -->
<!-- 		INSERT INTO projects  -->
<!-- 			(project_code, -->
<!-- 			project_name, -->
<!-- 			client, -->
<!-- 			phase_id, -->
<!-- 			organization_code, -->
<!-- 			start_date, -->
<!-- 			end_date, -->
<!-- 			flag, -->
<!-- 			created_by, -->
<!-- 			created_date) -->
<!-- 		VALUES  -->
<!-- 			(#projectCode#, -->
<!-- 			#projectName#, -->
<!-- 			#client#, -->
<!-- 			#phase#, -->
<!-- 			#organizationCode#, -->
<!-- 			#startDate#, -->
<!-- 			#endDate#, -->
<!-- 			'ACTIVE', -->
<!-- 			#session.userDomain#, -->
<!-- 			sysdate); -->
<!-- 	</insert> -->
	
</sqlMap>
